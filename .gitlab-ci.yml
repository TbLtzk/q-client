services:
  - docker:dind

variables:
  IMAGE_NAME: registry.gitlab.com/q-dev/q-client
  BUILD_IMAGE: $IMAGE_NAME:$CI_COMMIT_SHA
  BUILD_DOCKERFILE: Dockerfile.alltools

.before_script_template:
  before_script:
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/".insteadOf https://gitlab.com/
    - go env -w GOPRIVATE=gitlab.com/${CI_PROJECT_NAMESPACE}

stages:
  - test
  - build
  - release
  - deploy

test:
  image: golang:1.16.7
  stage: test
  extends: .before_script_template
  tags:
    - gitlab-org-docker
  only:
    - merge_requests
    - master
  script:
    - GO111MODULE=off go get -u github.com/jstemmer/go-junit-report
    - go test ./... -v 2>&1 | go-junit-report -set-exit-code > report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml

lint:
  image: golang:1.16.7
  stage: test
  extends: .before_script_template
  tags:
    - gitlab-org-docker
  only:
    - merge_requests
    - master
  script:
    - go run build/ci.go lint

# build and publish docker image to the registry
build:
  image: docker:latest
  stage: build
  tags:
    - gitlab-org-docker
  only:
    - master
    - latest
  script:
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
    # build binary
    - version=$(test -z $CI_COMMIT_TAG && echo $CI_COMMIT_SHA || echo $CI_COMMIT_TAG)
    - docker build --build-arg version="$version" --build-arg BUILD_TOKEN=${CI_JOB_TOKEN} --build-arg USERNAME=gitlab-ci-token -t $BUILD_IMAGE -f $BUILD_DOCKERFILE .
    - docker push $BUILD_IMAGE
    - docker tag $BUILD_IMAGE $IMAGE_NAME:latest
    - docker push $IMAGE_NAME:latest

# it's not really a good way to do this
# but ok for now
release:
  image: docker:latest
  stage: release
  tags:
    - gitlab-org-docker
  only:
    - tags
  script:
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
    - version=$(test -z $CI_COMMIT_TAG && echo $CI_COMMIT_SHA || echo $CI_COMMIT_TAG)
    - image=$IMAGE_NAME:$CI_COMMIT_TAG
    - docker build --build-arg version="$version" --build-arg BUILD_TOKEN=${CI_JOB_TOKEN} --build-arg USERNAME=gitlab-ci-token -t $BUILD_IMAGE -t $image -f $BUILD_DOCKERFILE .
    - docker push $BUILD_IMAGE
    - docker push $image

# deploy_devnet:
#   image: alpine
#   stage: deploy
#   tags:
#     - gitlab-org-docker
#   only:
#     - master
#   script:
#     - apk add openssh
#     - mkdir -p ~/.ssh
#     - echo "${DEV_SSH_PRIVATE_KEY}" | tr -d '\r' >> ~/.ssh/id_rsa
#     - chmod 600 ~/.ssh/id_rsa
#     - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
#     - sed -i "s@{{ .Image }}@${BUILD_IMAGE}@g" docker-compose.yaml    # replace image in docker-compose
#     - scp docker-compose.yaml ubuntu@$DEV_HOST:~/developer-edition/docker-compose.yaml
#     - ssh ubuntu@$DEV_HOST "docker login -u gitlab-ci-token -p ${CI_BUILD_TOKEN} registry.gitlab.com
#                             && cd developer-edition
#                             && docker-compose down
#                             && docker image prune -a -f
#                             && docker-compose up -d"
